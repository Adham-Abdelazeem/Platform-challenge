apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned-issuer
  annotations:
    argocd.argoproj.io/sync-wave: "5"                # as it is internal certifications we are not going to use "Let's Encrypt" option, which requires a public accessible domain
spec:
  selfSigned: {}
